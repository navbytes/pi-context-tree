{
	"name": "pi-context-tree",
	"type": "module",
	"version": "0.1.1",
	"description": "Git-style /branch /merge /crop workflow + context panel for pi sessions — keep your agent's context small, fresh, and relevant.",
	"keywords": [
		"pi-package",
		"pi",
		"pi-coding-agent",
		"pi-extension",
		"context-engineering",
		"context-management",
		"coding-agent",
		"llm",
		"tui",
		"branch",
		"merge",
		"crop"
	],
	"license": "MIT",
	"author": "Naveen (navbytes)",
	"homepage": "https://github.com/navbytes/pi-context-tree#readme",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/navbytes/pi-context-tree.git"
	},
	"bugs": {
		"url": "https://github.com/navbytes/pi-context-tree/issues"
	},
	"image": "https://cdn.jsdelivr.net/gh/navbytes/pi-context-tree@main/docs/assets/demo.gif",
	"video": "https://cdn.jsdelivr.net/gh/navbytes/pi-context-tree@main/docs/assets/demo.mp4",
	"pi": {
		"extensions": ["./extensions"]
	},
	"workspaces": ["packages/*"],
	"engines": {
		"node": ">=22.19.0"
	},
	"scripts": {
		"prepare": "node -e \"require('fs').existsSync('packages/core')&&require('child_process').execSync('npm run build',{stdio:'inherit'})\"",
		"build": "npm run build -w @pi-context-tree/core && npm run build -w @pi-context-tree/tui && npm run build -w @pi-context-tree/pitree",
		"test": "npm run build && npm run test --workspaces --if-present",
		"check": "npm run check:types && npm run check:lint",
		"check:types": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/tui/tsconfig.json --noEmit && tsc -p packages/extension/tsconfig.json --noEmit && tsc -p packages/pitree/tsconfig.json --noEmit",
		"check:lint": "biome check .",
		"fixtures": "node fixtures/generate.ts"
	},
	"dependencies": {
		"@pi-context-tree/extension": "0.1.1",
		"@types/node": "^22.10.0",
		"typescript": "^5.7.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"vitest": "^4.1.8"
	},
	"files": ["extensions"]
}
