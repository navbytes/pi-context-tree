{
	"name": "@pi-context-tree/pitree",
	"version": "0.0.1",
	"type": "module",
	"description": "Forest CLI across all pi projects (dangling-branch detection) + standalone read-only panel",
	"bin": {
		"pitree": "./dist/cli.js"
	},
	"main": "./dist/index.js",
	"files": ["dist"],
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"test": "vitest --run"
	},
	"dependencies": {
		"@earendil-works/pi-tui": "0.79.1",
		"@pi-context-tree/core": "*",
		"@pi-context-tree/tui": "*",
		"chalk": "^5.3.0"
	},
	"engines": {
		"node": ">=22.19.0"
	}
}
